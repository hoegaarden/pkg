#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: #@ data.values.name if hasattr(data.values, "name") else "test"
data:
  some: data
  foo: #@ yaml.encode(data.values)
